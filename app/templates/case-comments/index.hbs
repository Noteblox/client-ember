<h1>Issue-comments list</h1>

<p>
  {{link-to "New Issue-comment" "case-comments.new" }}
</p>

{{#if model.length}}
  <table>
    <thead>
      <tr>
        <th>
          Name
        </th>
        <th>
          Title
        </th>
        <th>
          Description
        </th>
        <th>
          Visibility
        </th>
        <th colspan="3">
          Actions
        </th>
      </tr>
    </thead>
    <tbody>
      {{#each model as |case-comment|}}
        <tr>
          <td>
            {{case-comment.name}}
          </td>
          <td>
            {{case-comment.title}}
          </td>
          <td>
            {{case-comment.description}}
          </td>
          <td>
            {{case-comment.visibility}}
          </td>
          <td>
            {{link-to "Edit" "case-comments.edit" case-comment}}
          </td>
          <td>
            {{link-to "Show" "case-comments.show" case-comment}}
          </td>
          <td>
            <a href="#" {{action "remove" case-comment}}>Remove</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <p id="blankslate">
    No Issue-comments found
  </p>
{{/if}}
