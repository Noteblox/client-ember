<!-- app/templates/spaces/index.hbs -->
<div class="layout-fill ayout-align-center-start ">


    {{#if model.length}}

        {{#each model as |item|}}

          {{#paper-card as |card|}}
            {{#card.title as |title|}}
              {{#title.text as |text|}}
                {{#text.headline}}{{tem.title}}{{/text.headline}}
                {{#text.subhead}}{{item.name}} created {{moment-to-now item.createdDate}}{{/text.subhead}}
              {{/title.text}}
              {{#title.media size="md"}}
                <div class="card-media">
                  {{user-avatar name=item.name image=item.avatarUrl}}
                </div>
              {{/title.media}}
            {{/card.title}}
            {{#card.actions}}
              {{#paper-button}}Action 1{{/paper-button}}
              {{#paper-button}}Action 2{{/paper-button}}
            {{/card.actions}}
          {{/paper-card}}

      {{/each}}
    {{else}}

        <p>
         This spaces found
        </p>

    {{/if}}


</div>