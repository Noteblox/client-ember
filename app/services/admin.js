import Ember from 'ember';
import EmberAdminServiceAdmin from 'ember-admin/services/admin';

export default EmberAdminServiceAdmin.extend({
    /*
    // see https://github.com/dockyard/ember-admin#user-content-including--excluding-models
    includedModels: null,
    excludedModels: null

    // see https://github.com/dockyard/ember-admin#user-content-including--excluding-columns
    includedColumns: {
        'person': ['name', 'age']
    },
    excludedColumns: {
        'project': ['dateCreated']
    }
    */
});